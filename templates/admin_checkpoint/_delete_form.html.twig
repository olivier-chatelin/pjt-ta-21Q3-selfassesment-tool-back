<form method="post" action="{{ path('admin_delete', {'id': checkpoint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checkpoint.id) }}">
    <button type="submit" class="btn btn-wild-dark w-100 mb-2">Delete</button>
</form>
