{% extends 'base.html.twig' %}

{% block title %}Edit Checkpoint{% endblock %}

{% block body %}
    <div class="container w-50">
        <h1>Edit Checkpoint n°{{ checkpoint.number }} {{ checkpoint.curriculum.name }}</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-wild-yellow ">Update</button>
        {{ form_end(form) }}
        <a href="{{ path('admin_home') }}">back to list</a>
    </div>

{% endblock %}
